<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.board.dao.IAttatchDAO">
	<insert id="insertAttaches" parameterType="BoardVO">
		<selectKey order="BEFORE" keyProperty="startAttNo" resultType="int">
			SELECT NVL(MAX(ATT_NO), 0)+1
			FROM ATTATCH
		</selectKey>
		INSERT ALL 
		<foreach collection="attatchList" item="attatch" index="idx">
			INTO attatch (
			    ATT_NO,    ATT_SAVENAME,
			    ATT_FILENAME,    ATT_MIME,
			    BO_NO,    ATT_FILESIZE,
			    ATT_FANCY
			) VALUES (
				#{startAttNo,jdbcType=NUMERIC}+#{idx},    #{attatch.att_savename,jdbcType=VARCHAR},
			    #{attatch.att_filename,jdbcType=VARCHAR},    #{attatch.att_mime,jdbcType=VARCHAR},
			    #{bo_no,jdbcType=NUMERIC},    #{attatch.att_filesize,jdbcType=NUMERIC},
			    #{attatch.att_fancy,jdbcType=VARCHAR}
			)
		</foreach>
		SELECT * FROM DUAL
	</insert>
	<delete id="deleteAttatches" parameterType="BoardVO">
		DELETE FROM ATTATCH
		<where>
			<if test="delAttNos!=null and delAttNos.length gt 0">
				ATT_NO IN 
				<foreach collection="delAttNos" item="attNo" open="(" close=")" separator=",">
					#{attNo}
				</foreach>	
			</if>
			<if test="delAttNos==null or delAttNos.length eq 0">
				BO_NO = #{bo_no}
			</if>
		</where>
			
	</delete>
	<select id="selectAttach" parameterType="int" resultType="AttatchVO">
		SELECT 
			ATT_NO,    ATT_SAVENAME,
		    ATT_FILENAME,    ATT_MIME,
		    BO_NO,    ATT_FILESIZE,
		    ATT_FANCY, ATT_DOWNCOUNT
		FROM ATTATCH
		WHERE ATT_NO = #{att_no}
	</select>
	<update id="incrementDownCount" parameterType="int">
		UPDATE ATTATCH 
		SET ATT_DOWNCOUNT = ATT_DOWNCOUNT + 1
		WHERE ATT_NO = #{att_no}
	</update>
</mapper>









